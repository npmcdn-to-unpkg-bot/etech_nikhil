import { Component, Input } from '@angular/core';
import { Detail } from './variables/variable';

@Component({
  selector: 'my-list-detail',
  templateUrl: 'app/list-detail.component.html'
})
export class ListDetailComponent {
  @Input() list: Detail;
}

